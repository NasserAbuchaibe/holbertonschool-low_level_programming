#include "holberton.h"
#include <stdio.h>

/**
 * _strcmp - compare two strings
 * @dest: string 1
 * @src: string 2
 * @n: integer
 * Return: dest
 */

int _strcmp(char *s1, char *s2)
{
	int x, y;

	for (x = 0; s1[x] != '\0'; x++)
	{
	}
	for (y = 0; s2[y] != '\0'; y++)
	{
	}
	return(x - y);
}

