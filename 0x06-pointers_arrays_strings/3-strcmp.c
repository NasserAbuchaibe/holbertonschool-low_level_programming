#include "holberton.h"
#include <stdio.h>

/**
 * _strcmp - compare two strings
 * @s1: string 1
 * @s2: string 2
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int x, y, z;

	for (x = 0; s1[x] != '\0'; x++)
	{
	}
	for (y = 0; s2[y] != '\0'; y++)
	{
	}
	z = x - y;
	return (z);
}

